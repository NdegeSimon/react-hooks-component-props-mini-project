// import React from "react";

// function Header({ name="Underreacted" }) {  // Destructure props properly
//   return (  // Parentheses needed for multi-line JSX
//     <div>
//       <h1>{name}</h1>  
//     </div>
//   );
// }

// export default Header;  // Don't forget to export
import React from "react";

function Header() {
  return (
    <header>
      <h1>Underreacted</h1>
    </header>
  );
}

export default Header;